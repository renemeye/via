<%= form_with(model: plus_city_zone, local: true) do |form| %>
  <% if plus_city_zone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(plus_city_zone.errors.count, "error") %> prohibited this plus_city_zone from being saved:</h2>

      <ul>
      <% plus_city_zone.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :dtzid %>
    <%= form.text_field :dtzid %>
  </div>

  <div class="field">
    <%= form.label :city_name %>
    <%= form.text_field :city_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
